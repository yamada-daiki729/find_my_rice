<div class="">
  <p class="" >お米総選挙のぺーじやで</p>

  <% @ranking_data.first(7).each_with_index do |rice, i|%>
    <div class="flex my-4">
      <h3 class=""><%=  "No.#{i+1}" %></h3>
      <%= link_to rice_path(rice,keyword: rice.name ) do %>
        <h2 class="ml-4"><%= "#{rice.name}" %></h2>
      <% end %>
    </div>
  <%end%>
</div>

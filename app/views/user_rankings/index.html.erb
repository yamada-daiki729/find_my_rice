<% content_for(:title, "お米総選挙") %>
<div class="container my-8 w-3/4 bg-gray-50 rounded-2xl border-solid border-4 border-light-blue-500 ...">
  <p class="text-center text-4xl m-8 font-extrabold" >お米総選挙 </p>
  <p class="text-center " >ユーザー皆さんの米(まい)ランキングから集計されたランキングです！</p>
  <p class="text-center " >気になったお米をクリックしてみよう！！</p>
    <% @ranking_data.first(7).each_with_index do |rice, i|%>
    <div class="w-1/4 mx-auto">
      <div class="flex my-4 justify-between">
        <% if i ==0 or i ==1 or i ==2 %>
          <%= image_tag "kkrn_icon_ranking_#{i+1}.png", size:"40x40"%>
        <% else %>
          <p class=""><%=  "No.#{i+1}" %></p>
        <% end %>
        <%= link_to rice_path(rice,keyword: rice.name ), class:"mx-auto my-auto" do %>
          <p class=""><%= "#{rice.name}" %></p>
        <% end %>
      </div>
    </div>
    <%end%>
</div>
